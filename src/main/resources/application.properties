# Application
spring.application.name=PilgrimTemple

# Server
server.port=8081
server.servlet.context-path=/pilgrim

# MySQL Database
spring.datasource.url=jdbc:mysql://localhost:3306/pilgrim_temple?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Yogi@1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
# Note: 'update' mode doesn't always modify existing columns
# Use 'create' temporarily to recreate tables with correct schema, then change back to 'update'
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.generate-ddl=true

# JSP View Resolver
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

# Thymeleaf Configuration (for Accommodation module)
# Note: JSP view resolver takes priority (configured in WebConfig)
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8

# Static Resources
spring.web.resources.static-locations=classpath:/static/

# File Upload
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB
file.upload.directory=C:/pilgrim-uploads/

# Email Configuration (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=kvsudeep014@gmail.com
spring.mail.password=jwed npsm gtnk mrvi
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# JWT Configuration
app.jwt.secret=PiligrimAccommodationSecretKeyForJWTTokenGeneration2024VerySecureKey
app.jwt.expiration=86400000

# OTP Configuration
app.otp.expiration=300000

# Session Timeout (30 minutes)
server.servlet.session.timeout=30m

# Razorpay Configuration
razorpay.key.id=rzp_test_RIlD5bEKRjyn3h
razorpay.key.secret=Ltg6uo9vI8TiFMVfj2cGm4I8

# OpenAI Configuration (for Chat Service)
openai.api.key=${OPENAI_API_KEY:}

# Logging
logging.level.com.pilgrim=DEBUG
logging.level.com.piligrimaccomadation=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

